import 'package:flutter/material.dart';

const primaryColor = Color.fromARGB(255, 45, 43, 51);
const secondaryColor = Color(0xFFEFF0FF);
const bgColor = Color.fromARGB(255, 29, 31, 35);
const cardColor = Color.fromARGB(255, 64, 62, 73);

const defaultPadding = 16.0;

const Duration kExpand = Duration(milliseconds: 200);
